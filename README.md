# hjhsxt
<!DOCTYPE html><br/><html lang="zh-CN"><br/><head><br/>    <meta charset="UTF-8"><br/>    <meta name="viewport" content="width=device-width, initial-scale=1.0"><br/>    <title>焊剂回收记录系统</title><br/>    <style><br/>        :root {<br/>            --primary-color: #2196F3;<br/>            --bg-color: #f4f7f6;<br/>            --card-bg: #ffffff;<br/>            --text-color: #333;<br/>        }<br/>        body {<br/>            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;<br/>            background-color: var(--bg-color);<br/>            margin: 0;<br/>            padding: 15px;<br/>            color: var(--text-color);<br/>        }<br/>        .container {<br/>            max-width: 600px;<br/>            margin: 0 auto;<br/>            padding-bottom: 50px;<br/>        }<br/>        h1 { text-align: center; color: #444; font-size: 1.4rem; margin-bottom: 15px; }<br/>        <br/>        /* 卡片样式 */<br/>        .card {<br/>            background: var(--card-bg);<br/>            border-radius: 12px;<br/>            padding: 18px;<br/>            margin-bottom: 15px;<br/>            box-shadow: 0 2px 8px rgba(0,0,0,0.08);<br/>        }<br/><br/>        /* 统计面板 */<br/>        .dashboard {<br/>            display: flex;<br/>            justify-content: space-around;<br/>            text-align: center;<br/>            background: linear-gradient(135deg, #2196F3, #21CBF3);<br/>            color: white;<br/>        }<br/>        .stat-box h3 { margin: 0; font-size: 28px; text-shadow: 0 1px 2px rgba(0,0,0,0.1); }<br/>        .stat-box p { margin: 5px 0 0; opacity: 0.9; font-size: 13px; }<br/><br/>        /* 表单样式 */<br/>        .form-group { margin-bottom: 15px; }<br/>        label { display: block; margin-bottom: 6px; font-weight: 600; font-size: 14px; color: #555; }<br/>        <br/>        input, select {<br/>            width: 100%;<br/>            padding: 12px;<br/>            border: 1px solid #ddd;<br/>            border-radius: 8px;<br/>            box-sizing: border-box;<br/>            font-size: 16px;<br/>            background-color: #fff;<br/>            appearance: none; /* 移除默认样式，特别是iOS */<br/>        }<br/>        /* 针对时间选择器的优化 */<br/>        input[type="datetime-local"] {<br/>            font-family: inherit;<br/>            color: #333;<br/>        }<br/><br/>        button {<br/>            width: 100%;<br/>            padding: 14px;<br/>            background-color: var(--primary-color);<br/>            color: white;<br/>            border: none;<br/>            border-radius: 8px;<br/>            font-size: 16px;<br/>            cursor: pointer;<br/>            font-weight: bold;<br/>            box-shadow: 0 2px 5px rgba(33, 150, 243, 0.3);<br/>        }<br/>        button:active { transform: scale(0.98); }<br/>        <br/>        /* 表格样式 */<br/>        .table-container { overflow-x: auto; }<br/>        table {<br/>            width: 100%;<br/>            border-collapse: collapse;<br/>            margin-top: 5px;<br/>            font-size: 14px;<br/>        }<br/>        th { text-align: left; color: #888; font-weight: normal; padding: 8px 5px; border-bottom: 1px solid #eee; }<br/>        td { padding: 12px 5px; border-bottom: 1px solid #f0f0f0; vertical-align: middle; }<br/>        <br/>        .time-cell div:first-child { font-weight: bold; color: #333; }<br/>        .time-cell div:last-child { font-size: 12px; color: #999; margin-top: 2px; }<br/>        <br/>        .weight-cell { font-weight: bold; color: var(--primary-color); font-size: 16px; }<br/>        <br/>        .delete-btn {<br/>            color: #ff5252;<br/>            border: 1px solid #ff5252;<br/>            padding: 4px 8px;<br/>            border-radius: 4px;<br/>            font-size: 12px;<br/>            cursor: pointer;<br/>            background: transparent;<br/>        }<br/><br/>        /* 按钮组 */<br/>        .action-buttons {<br/>            display: flex;<br/>            gap: 10px;<br/>            margin-top: 15px;<br/>        }<br/>        .btn-export { background-color: #4CAF50; box-shadow: 0 2px 5px rgba(76, 175, 80, 0.3); }<br/>        .btn-danger { background-color: #fff; color: #ff5252; border: 1px solid #ff5252; box-shadow: none; }<br/>    </style><br/></head><br/><body><br/><br/><div class="container"><br/>    <h1>🛠️ 焊剂回收记录</h1><br/><br/>    <div class="card dashboard"><br/>        <div class="stat-box"><br/>            <h3 id="todayTotal">0.0</h3><br/>            <p>今日回收 (kg)</p ><br/>        </div><br/>        <div class="stat-box"><br/>            <h3 id="allTotal">0.0</h3><br/>            <p>累计总重 (kg)</p ><br/>        </div><br/>    </div><br/><br/>    <div class="card"><br/>        <div class="form-group"><br/>            <label for="recordTime"><span class="emoji emoji1f552"></span> 回收时间</label><br/>            <input type="datetime-local" id="recordTime"><br/>        </div><br/>        <div class="form-group"><br/>            <label for="workerName">👤 作业人员姓名</label><br/>            <input type="text" id="workerName" placeholder="例如：张三" list="common-names"><br/>            <datalist id="common-names"><br/>                <option value="张三"><br/>                <option value="李四"><br/>                <option value="王五"><br/>                <option value="早班一组"><br/>            </datalist><br/>        </div><br/>        <div class="form-group"><br/>            <label for="weight">⚖️ 回收重量 (kg)</label><br/>            <input type="number" id="weight" placeholder="输入数字，如 25.5" step="0.01" inputmode="decimal"><br/>        </div><br/>        <button onclick="addRecord()">确认提交</button><br/>    </div><br/><br/>    <div class="card"><br/>        <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:10px;"><br/>            <h3 style="margin:0; font-size:16px;">最近记录</h3><br/>            <span style="font-size:12px; color:#999" id="recordCount">0 条</span><br/>        </div><br/>        <br/>        <div class="table-container"><br/>            <table id="recordTable"><br/>                <thead><br/>                    <tr><br/>                        <th width="35%">时间/日期</th><br/>                        <th width="25%">姓名</th><br/>                        <th width="20%">重量</th><br/>                        <th width="20%" style="text-align:right">操作</th><br/>                    </tr><br/>                </thead><br/>                <tbody><br/>                    </tbody><br/>            </table><br/>        </div><br/>        <br/>        <div class="action-buttons"><br/>            <button class="btn-export" onclick="exportToExcel()">📥 导出表格</button><br/>            <button class="btn-danger" onclick="clearAllData()">清空数据</button><br/>        </div><br/>    </div><br/></div><br/><br/><script><br/>    // 初始化数据<br/>    let records = JSON.parse(localStorage.getItem('flux_records_v2')) || [];<br/>    <br/>    // 页面加载时，设置时间框默认为当前时间<br/>    setDefaultTime();<br/>    updateUI();<br/><br/>    // 设置时间输入框为当前时间<br/>    function setDefaultTime() {<br/>        const now = new Date();<br/>        // 需要处理时区偏差，转为 ISO 格式的前半部分<br/>        now.setMinutes(now.getMinutes() - now.getTimezoneOffset());<br/>        document.getElementById('recordTime').value = now.toISOString().slice(0, 16);<br/>    }<br/><br/>    // 添加记录<br/>    function addRecord() {<br/>        const timeInput = document.getElementById('recordTime');<br/>        const nameInput = document.getElementById('workerName');<br/>        const weightInput = document.getElementById('weight');<br/>        <br/>        const timeStr = timeInput.value;<br/>        const name = nameInput.value.trim();<br/>        const weight = parseFloat(weightInput.value);<br/><br/>        if (!timeStr || !name || isNaN(weight) || weight <= 0) {<br/>            alert('请完整填写时间、姓名和有效的重量！');<br/>            return;<br/>        }<br/><br/>        const recordDate = new Date(timeStr);<br/>        <br/>        const newRecord = {<br/>            id: Date.now(), <br/>            name: name,<br/>            weight: weight,<br/>            // 存储完整的时间戳用于排序<br/>            timestamp: recordDate.getTime(),<br/>            // 格式化显示用的字符串<br/>            dateStr: recordDate.toLocaleDateString(),<br/>            timeStr: recordDate.toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'})<br/>        };<br/><br/>        // 添加到数组开头<br/>        records.unshift(newRecord);<br/>        // 按时间倒序重新排序（防止补录以前的数据显示乱了）<br/>        records.sort((a, b) => b.timestamp - a.timestamp);<br/><br/>        saveData();<br/>        <br/>        // 重置输入框，保留时间为刚填的（方便连续录入同时间段），或者重置为当前时间？这里选择重置为当前时间<br/>        nameInput.value = '';<br/>        weightInput.value = '';<br/>        setDefaultTime(); <br/>        <br/>        // 轻微提示<br/>        // alert('✅ 记录已添加'); // 嫌弹窗烦可以注释掉这行<br/>    }<br/><br/>    // 删除单条记录<br/>    function deleteRecord(id) {<br/>        if (confirm('确定要删除这条记录吗？')) {<br/>            records = records.filter(r => r.id !== id);<br/>            saveData();<br/>        }<br/>    }<br/><br/>    // 清空所有<br/>    function clearAllData() {<br/>        if (confirm('<span class="emoji emoji26a0"></span>️ 警告：这将删除所有历史记录！确定要清空吗？')) {<br/>            records = [];<br/>            saveData();<br/>        }<br/>    }<br/><br/>    // 保存并刷新<br/>    function saveData() {<br/>        localStorage.setItem('flux_records_v2', JSON.stringify(records));<br/>        updateUI();<br/>    }<br/><br/>    // 更新界面<br/>    function updateUI() {<br/>        const tbody = document.querySelector('#recordTable tbody');<br/>        tbody.innerHTML = '';<br/><br/>        let todaySum = 0;<br/>        let totalSum = 0;<br/>        <br/>        // 获取今天的日期字符串，用于比较<br/>        const now = new Date();<br/>        const todayStr = now.toLocaleDateString();<br/><br/>        records.forEach(r => {<br/>            // 统计<br/>            totalSum += r.weight;<br/>            // 检查记录的时间戳是否属于今天<br/>            const rDate = new Date(r.timestamp).toLocaleDateString();<br/>            if (rDate === todayStr) {<br/>                todaySum += r.weight;<br/>            }<br/><br/>            // 渲染表格<br/>            const row = `<br/>                <tr><br/>                    <td class="time-cell"><br/>                        <div>${r.dateStr}</div><br/>                        <div>${r.timeStr}</div><br/>                    </td><br/>                    <td>${r.name}</td><br/>                    <td class="weight-cell">${r.weight}</td><br/>                    <td style="text-align:right"><br/>                        <button class="delete-btn" onclick="deleteRecord(${r.id})">删除</button><br/>                    </td><br/>                </tr><br/>            `;<br/>            tbody.innerHTML += row;<br/>        });<br/><br/>        document.getElementById('todayTotal').innerText = todaySum.toFixed(1);<br/>        document.getElementById('allTotal').innerText = totalSum.toFixed(1);<br/>        document.getElementById('recordCount').innerText = records.length + " 条";<br/>    }<br/><br/>    // 导出 Excel (CSV)<br/>    function exportToExcel() {<br/>        if (records.length === 0) {<br/>            alert('没有数据可以导出');<br/>            return;<br/>        }<br/><br/>        // CSV 头部<br/>        let csvContent = "\uFEFF日期,时间,姓名,重量(kg)\n";<br/><br/>        records.forEach(r => {<br/>            // 确保导出格式清晰
